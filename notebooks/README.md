# Notebooks

Use this folder for exploratory analysis and experimentation.

Suggested notebooks:

- `01_eda.ipynb` for class imbalance and feature distribution checks
- `02_modeling_baseline.ipynb` for first baseline experiments

Keep training logic duplicated in `src/` modules once stabilized.
